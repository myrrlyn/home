<%= if @page do %>
	<article>
		<%= @page.content |> raw %>
	</article>
<% end %>
<%= for {heading, images} <- @gallery do %>
	<.images heading={heading} images={images} root={@directory} />
<% end %>
