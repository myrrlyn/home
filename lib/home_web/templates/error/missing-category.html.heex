<article>
	<h1 id="title"><%= @name %></h1>
	<p>
		The <code><%= @slug %></code> category does not have an index file. An
		autogenerated page listing is provided:
	</p>
	<ol reversed>
		<%= for {url, meta} <- @pages do %>
			<li>
				<%= link meta.title, to: url, class: "title" %>
				<%= "(#{Timex.format!(meta.date, "{ISOdate}")})" %>
				<br />
				<%= meta.summary |> Earmark.as_html! |> raw %>
			</li>
		<% end %>
	</ol>
</article>
