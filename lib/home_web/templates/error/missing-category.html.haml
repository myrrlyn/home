
%article
	%h1#title= @name
	%p
		The
		%code= @slug
		category does not have an index file. An autogenerated page listing is
		provided:
	%ol{reversed=""}
		- for {url, meta} <- @pages do
			%li
				%a.title{href: "#{url}"}= meta.title
				= "(#{Timex.format!(meta.date, "{ISOdate}")})"
				%br
				= meta.summary |> Earmark.as_html! |> raw
